import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    justifyContent: 'center',
  },
  error: {
    color: 'red',
    marginBottom: 10,
    marginHorizontal: 10,
  },
  tabStyle: {
    width: '100%',
    height: 60,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
